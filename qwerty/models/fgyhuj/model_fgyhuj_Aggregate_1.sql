{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_fgyhuj_post_Aggregate_1",
    "database": "database",
    "schema": "default"
  })
}}

WITH Aggregate_1 AS (

  SELECT * 
  
  FROM `` AS in0

)

SELECT *

FROM Aggregate_1
